/*
 * This file contains handling code for the application routes specified in app.js
 */

/*
 * Displays 'report picker' interface.
 * If reportCode is specified, prefills it with given reportCode.
 */
function displayReportPicker(appContainer, reportCode=null) {
	// TODO implement
}

/*
 * Displays 'fight picker' interface.
 * If fightNum is specified, preselects with given fight
 */
function displayFightPicker(appContainer, reportCode, fightNum=null) {
	// TODO implement
}

/*
 * Analyzes the given fight,
 * fetching data, feeding through analyzers, posting results.
 */
function analyzeFight(appContainer, reportCode, fightNum) {
	// TODO implement
}